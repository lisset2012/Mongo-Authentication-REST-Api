<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>Update profile</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" type="text/css" media="screen" href="/main.css" />
</head>
<body>
        <nav class="nav">
            <div>
                Hi <%=user.name.split(' ')[0]%>
            </div>
            <div>
                <a class="" href="/profile">Profile</a>
                <a class="" href="/sign-out">Sign Out</a>
            </div>
        </nav>

        <div class="container">

                <% if (message.lenght) {%>
                    <div class="alert">
                        <!-- this mesaage comes from routes/views when rendea la pagina y lanza el mensage con flash -->
                        <%= message%>
                    </div>
                <%}%>
                
            <form action="/sign-up" method="POST">
    
                <label for="name">Name</label>
                    <div class="input-container">
                        <input type="text" name="name" class="form-input" value="<%=user.name%>" required/>
                    </div>
    
                <label for="email">Email</label>
                    <div class="input-container">
                        <input type="email" name="email" class="form-input"  value="<%=user.name%>" required/>
                    </div>
    
                <label for="password">Current Password</label>
                    <div class="input-container">
                        <input type="password" name="password" class="form-input"   required/>
                    </div>

                    <label for="password">New Password</label>
                    <div class="input-container">
                        <input type="password" name="password" class="form-input"   required/>
                    </div>
    
                <label for="password_confirmation">New Password confirmation</label>
                    <div class="input-container">
                        <input type="password" name="password_confirmation" class="form-btn"   required/>
                    </div>
            
                <button type="submit">Update</button>
            </form>
    
            <div class="links-container">
                Already have an account?<a href="/profile">Sign in</a>
            </div>
        </div>
    <script src="/main.js"></script>
</body>
</html>